using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace DoubleDrift
{
    public class TrafficPool : Pooler
    {
        public override void Initialize(string tag, Transform parent)
        {
            base.Initialize(tag, parent);
        }
    }
}
